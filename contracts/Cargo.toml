[package]
name = "rust-lints-contracts"
version = "0.1.0"
edition = "2024"
description = "Co-located source contracts consumed by rust-lints"
license = "MIT OR Apache-2.0"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
quote = "1"
syn = { version = "2", features = ["full"] }
